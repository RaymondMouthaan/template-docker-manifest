# make use of vm's 
sudo: 'required'

# have the docker service set up (we'll update it later)
services:
  - 'docker'

language:
  - 'bash'

env:
  - QEMU_VERSION=v2.9.1-1

before_install:
  # prepare the machine before any code installation scripts
  - './.travis/main.sh'
  # Prepare qemu
  - docker run --rm --privileged multiarch/qemu-user-static:register --reset
  # Get qemu-arm-static binary
  - mkdir tmp
  - >
    pushd tmp &&
    curl -L -o qemu-arm-static.tar.gz https://github.com/multiarch/qemu-user-static/releases/download/$QEMU_VERSION/qemu-arm-static.tar.gz &&
    tar xzf qemu-arm-static.tar.gz &&
    curl -L -o qemu-aarch64-static.tar.gz https://github.com/multiarch/qemu-user-static/releases/download/$QEMU_VERSION/qemu-aarch64-static.tar.gz &&
    tar xzf qemu-aarch64-static.tar.gz &&
    popd

script:
  - 'make test'
  - 'make image'

after_success:
  - if [[ "$TRAVIS_BRANCH" == "master" ]]; then
      echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin ;
      make push-image ;
      
      docker manifest create raymondmm/docker-manifest:v1 \
        docker-manifest-arm-linux:v1 ;
        
      docker manifest annotate raymondmm/docker-manifest:v1 raymondmm/docker-manifest-arm-linux --arch arm ;
      docker manifest push raymondmm/docker-manifest:v1 ;
      docker manifest inspect raymondmm/docker-manifest ;
    fi

# don't notify me when things fail
notifications:
  email: false
